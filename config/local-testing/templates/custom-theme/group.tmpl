{{define "group"}}
<section class="fc-group">
  <details open>
    <summary class="fc-group-summary">
      <span class="fc-group-meta">
        <i class="fa-solid fa-folder-open" aria-hidden="true"></i>
        <h2 class="fc-group-name">{{.Name}}</h2>
        <span class="fc-group-count">{{len .Links}}</span>
      </span>
      <i class="fa-solid fa-chevron-down fc-group-chevron" aria-hidden="true"></i>
    </summary>
    <div class="fc-group-panel">
      <div class="fc-cards">
        {{range .Links}}
          {{template "link" .}}
        {{end}}
      </div>
    </div>
  </details>
</section>
{{end}}
