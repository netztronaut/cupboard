---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bookmarkgroups.dashboard.netztronaut.de
spec:
  group: dashboard.netztronaut.de
  names:
    kind: BookmarkGroup
    listKind: BookmarkGroupList
    plural: bookmarkgroups
    singular: bookmarkgroup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BookmarkGroup is the Schema for the bookmarkgroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BookmarkGroup
            properties:
              links:
                description: Links are the dashboard entries shown under this group.
                items:
                  description: BookmarkLink describes a link entry rendered in the
                    dashboard.
                  properties:
                    groups:
                      description: |-
                        Groups restricts this link to users who belong to at least one listed group.
                        When omitted, the link is visible to every user.
                      items:
                        type: string
                      type: array
                    icon:
                      description: |-
                        Icon identifies an icon for this link.
                        It can be an absolute URL (http/https/data), an icon-set key (e.g. fa-home),
                        a relative URL, or a filename resolved by an asset service.
                      maxLength: 2048
                      type: string
                    name:
                      description: Name is the display name of this link.
                      minLength: 1
                      type: string
                    networkRestricted:
                      description: |-
                        NetworkRestricted indicates this link is only reachable in private networks.
                        Adapted from Forecastle's API for backward-compatibility.
                      type: boolean
                    properties:
                      additionalProperties:
                        type: string
                      description: Properties allows free-form metadata, compatible
                        with Forecastle.
                      type: object
                    target:
                      description: |-
                        Target controls where the URL is opened.
                        Defaults to "_self".
                      enum:
                      - _self
                      - _blank
                      - _parent
                      - _top
                      type: string
                    url:
                      description: URL is the direct target URL for this link.
                      maxLength: 2048
                      type: string
                    urlFrom:
                      description: |-
                        URLFrom resolves URL dynamically from Kubernetes resources.
                        Supports Forecastle-compatible source fields.
                      properties:
                        httpRouteRef:
                          description: LocalObjectReference contains enough information
                            to locate an object in the same namespace.
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        ingressRef:
                          description: LocalObjectReference contains enough information
                            to locate an object in the same namespace.
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        ingressRouteRef:
                          description: LocalObjectReference contains enough information
                            to locate an object in the same namespace.
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        routeRef:
                          description: LocalObjectReference contains enough information
                            to locate an object in the same namespace.
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        serviceRef:
                          description: LocalObjectReference contains enough information
                            to locate an object in the same namespace.
                          properties:
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              name:
                description: |-
                  Name is the display name for this group in the dashboard.
                  When omitted, metadata.name is used.
                type: string
            required:
            - links
            type: object
          status:
            description: status defines the observed state of BookmarkGroup
            properties:
              conditions:
                description: Conditions represent the current state of the BookmarkGroup
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncedAt:
                description: LastSyncedAt indicates when the controller last reconciled
                  this object.
                format: date-time
                type: string
              linkCount:
                description: LinkCount is the number of links present in spec.links.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
